# ==============================================================================
# ☁️ HYBRID AWS CONFIGURATION
# ==============================================================================
# Kafka runs locally in Docker (MSK is too expensive ~$150/month)
# Databases use AWS managed services (RDS + ElastiCache)
#
# After running Terraform, copy the outputs here:
#   cd infrastructure/terraform/aws
#   terraform output rds_connection_string
#   terraform output redis_connection_string
# ==============================================================================

# -----------------------------------------------------------------------------
# AWS MANAGED DATABASES (From Terraform Output)
# -----------------------------------------------------------------------------

# Amazon RDS PostgreSQL
# Get from: terraform output rds_connection_string
TIMESCALE_URL=postgresql://trading:YOUR_PASSWORD@your-rds-endpoint.us-east-1.rds.amazonaws.com:5432/nifty50

# Amazon ElastiCache Redis
# Get from: terraform output redis_connection_string
REDIS_URL=redis://your-elasticache-endpoint.cache.amazonaws.com:6379

# -----------------------------------------------------------------------------
# KAFKA (Runs locally - no config needed, uses kafka:29092)
# -----------------------------------------------------------------------------
# Kafka is included in docker-compose.aws.yml, no external config required

# -----------------------------------------------------------------------------
# APPLICATION SECRETS
# -----------------------------------------------------------------------------

# Market Data Provider
MARKET_DATA_PROVIDER=mstock

# MStock API
MSTOCK_API_KEY=
MSTOCK_ACCESS_TOKEN=
MSTOCK_CLIENT_CODE=
MSTOCK_PASSWORD=
MSTOCK_TOTP_SECRET=

# Zerodha API (if using)
ZERODHA_API_KEY=
ZERODHA_ACCESS_TOKEN=

# Telegram Bot
TELEGRAM_BOT_TOKEN=

# Email Service
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
ADMIN_EMAIL=

# App Version
APP_VERSION=latest
