{
  "mcpServers": {
    "tradingview": {
      "command": "node",
      "args": ["layer-1-tradingview/src/server.js"]
    },
    "flattrade": {
      "command": "pnpm",
      "args": ["dlx", "@flattrade/mcp"],
      "env": {
        "FLATTRADE_API_KEY": "${FLATTRADE_API_KEY}",
        "FLATTRADE_API_SECRET": "${FLATTRADE_API_SECRET}"
      }
    }
  }
}
