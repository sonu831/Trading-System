Open-position book for the trading/positions screen.

```jsx
<PositionsTable positions={positions} />
```

A position with no stop-loss renders "NO STOP" in bold red — never silently blank. P&L is signed and colored (green/red), never color-only.
